name: solarflow_control
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.32.0       # Firebase Core (obligatorio)
  firebase_auth: ^4.11.0       # Autenticación con Firebase
  #google_sign_in: ^6.1.5 
  #sign_in_button: ^3.2.0      # Login con Google
  #flutter_svg: ^2.0.7          # Opcional: Para mostrar el logo de Google
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
